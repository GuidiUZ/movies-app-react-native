type apiResult = {
    key: number,
    image: string,
    title: string,
}

type imageHero = {
    homeImage: string,
}

type movies = {
    title: string,
    image: string,
    movieTitle : string,
}

type items = {
    item : string,
    movieData : string
}

export {apiResult, imageHero, movies, items};